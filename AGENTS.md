# Agent instructions

The agent instructions for this repository live in **[`CLAUDE.md`](./CLAUDE.md)**.
Read that file first — it applies to every agent working here, Codex included.

Architectural decisions and their reasoning are in
**[`docs/DECISIONS.md`](./docs/DECISIONS.md)**. Read it before proposing any change
to architecture, dependencies, or scope.

This file is a pointer, kept deliberately thin so the two never drift apart.
